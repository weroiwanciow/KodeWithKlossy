import Cocoa

var str = "Hello, playground"

//write a function that accepts a # as a parameter and check the number is prime or not if it is prime return true if not return false

func number() {
    var primenum = 0
    
    if primenum != () {
        return true;
        }else {
        return false;
    }
    
    
    
}

